function antesDepois(){
    var usuario = parseInt(window.prompt('Digite um número inteiro qualquer: '))
    window.alert(`Antes de ${usuario}, temos o ${usuario - 1}\nDepois de ${usuario}, temos o ${usuario +1}`);
}